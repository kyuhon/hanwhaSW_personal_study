server:
    port: 8001
    tomcat:
        max-connections: 200    # ?? ?? ?? ?

#?? ??? ?? ??
spring:
    servlet:
        multipart:
            enabled: true
            max-file-size: 100MB    # ??? ? ? ?? ? ?? ??(default 1MB)
            max-request-size: 200MB # ??? ??? ? ??(default 10MB)
    web:
        resources:
            static-locations: file:///C:/uploadFiles/    # ?? ??? ??
            cache-period: 3600                          # ? ????? ?? ??(?, 1??)

filepath: C:/uploadFiles